# Verify Build

Run the build and report any errors.

## Instructions

1. Run the project's build command:
   - Try `npm run build` or `bun run build`
   - Check package.json if unclear

2. If build succeeds: Report success

3. If build fails:
   - Show the error output
   - Identify the root cause
   - Suggest a fix
